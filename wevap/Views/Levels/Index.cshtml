@model IEnumerable<wevap.Models.Level>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Stage)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Stage)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Level }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Level }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Level })
        </td>
    </tr>
}

</table>
